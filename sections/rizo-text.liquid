{%- comment -%}
  Words, three ways: body (reading), statement (one big thought), quote
  (someone's actual words, with who said them).
{%- endcomment -%}
<section class="s text" data-surface="{{ section.settings.surface }}" data-space="{{ section.settings.space }}" data-fog="{{ section.settings.fog | divided_by: 100.0 }}" data-style="{{ section.settings.style }}" data-align="{{ section.settings.align }}">
  <div class="text-inner">
    {%- if section.settings.label != blank -%}<p class="v-label">{{ section.settings.label | escape }}</p>{%- endif -%}
    {%- if section.settings.style == 'quote' -%}
      <blockquote>{{ section.settings.body | strip_html }}</blockquote>
      {%- if section.settings.cite != blank -%}<cite>{{ section.settings.cite | escape }}</cite>{%- endif -%}
    {%- else -%}
      {%- if section.settings.heading != blank -%}<h2 class="text-heading">{{ section.settings.heading | escape }}</h2>{%- endif -%}
      {%- if section.settings.body != blank -%}<div class="{% if section.settings.style == 'body' %}rte t-lead{% endif %} text-body">{{ section.settings.body }}</div>{%- endif -%}
    {%- endif -%}
    {%- if section.settings.link_label != blank -%}
      <a class="link" href="{{ section.settings.link }}"><span>{{ section.settings.link_label | escape }}</span>{% render 'icon', name: 'arrow' %}</a>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Text",
  "tag": "div",
  "class": "section-text",
  "settings": [
    { "type": "select", "id": "style", "label": "Style", "default": "body", "options": [{ "value": "body", "label": "Reading" }, { "value": "statement", "label": "Statement" }, { "value": "quote", "label": "Quote" }] },
    { "type": "text", "id": "label", "label": "Small label" },
    { "type": "text", "id": "heading", "label": "Heading" },
    { "type": "richtext", "id": "body", "label": "Text", "default": "<p>Say it once, plainly.</p>" },
    { "type": "text", "id": "cite", "label": "Who said it (quote)" },
    { "type": "text", "id": "link_label", "label": "Link" },
    { "type": "url", "id": "link", "label": "Link destination" },
    { "type": "select", "id": "align", "label": "Position", "default": "center", "options": [{ "value": "left", "label": "Left" }, { "value": "center", "label": "Centre column" }, { "value": "offset", "label": "Off-centre" }] },
    { "type": "select", "id": "surface", "label": "Surface", "default": "solid", "options": [{ "value": "solid", "label": "Solid" }, { "value": "raised", "label": "Raised" }, { "value": "open", "label": "Open sky" }, { "value": "paper", "label": "Paper" }] },
    { "type": "select", "id": "space", "label": "Space", "default": "m", "options": [{ "value": "s", "label": "Tight" }, { "value": "m", "label": "Normal" }, { "value": "l", "label": "Loose" }] },
    { "type": "range", "id": "fog", "label": "Fog", "min": 0, "max": 100, "step": 5, "unit": "%", "default": 0 }
  ],
  "presets": [{ "name": "Text" }]
}
{% endschema %}
