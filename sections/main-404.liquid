<section class="s lost" data-surface="open" data-fog="0.9" data-fog-bias="ground">
  <div class="wrap wrap--wide">
    <h1>{{ section.settings.heading | escape }}</h1>
    {%- if section.settings.text != blank -%}<p>{{ section.settings.text | escape }}</p>{%- endif -%}
    <div class="cluster">
      <a class="btn btn--solid" href="{{ routes.all_products_collection_url }}">Shop{% render 'icon', name: 'arrow' %}</a>
      <a class="link" href="{{ routes.root_url }}"><span>Home</span></a>
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Not found",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "It used to be here." },
    { "type": "text", "id": "text", "label": "Text", "default": "The link is old or the page moved." }
  ]
}
{% endschema %}
